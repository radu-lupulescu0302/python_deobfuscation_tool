# No third-party dependencies required.
# All modules used (ast, base64, zlib, binascii, codecs, re,
# importlib, threading) are part of the Python standard library.
#
# Minimum Python version: 3.9  (requires ast.unparse)
# Recommended:            3.11
#
# Optional — enables marshal.loads decompilation in the static phase:
# uncompyle6